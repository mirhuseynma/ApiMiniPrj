
namespace ApiMiniPrj.Persistence.Test.Services;

public class JwtServiceTests
{
    //[Fact]
    //public async Task GenerateRefreshTokenAsync_ShouldReturnBase64EncodedRandomToken()
    //{
    //    var service = new JwtService(null!, Options.Create(new JwtSetting
    //    {
    //        Issuer = "test-issuer",
    //        Audience = "test-audience",
    //        SecretKey = "test-secret-key-with-enough-length",
    //        ExpirationMinutes = 30
    //    }));

    //    var firstToken = await service.GenerateRefreshTokenAsync();
    //    var secondToken = await service.GenerateRefreshTokenAsync();

    //    Assert.NotEqual(firstToken, secondToken);
    //    Assert.Equal(32, Convert.FromBase64String(firstToken).Length);
    //    Assert.Equal(32, Convert.FromBase64String(secondToken).Length);
    //}
}
